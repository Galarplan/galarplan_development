<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form_inherit_manual_date" model="ir.ui.view">
        <field name="name">account.move.form.inherit.manual.date</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_date']" position="after">
                <field name="manual_date" attrs="{'invisible': [('move_type', '!=', 'out_refund')]}" />
                <field name="manual_document_number"
                    attrs="{'invisible': [('move_type', '!=', 'out_refund')]}" />
            </xpath>
        </field>
    </record>

</odoo>