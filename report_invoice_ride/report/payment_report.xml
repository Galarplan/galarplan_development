<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Definición del Reporte -->
    <record id="action_payment_receipt" model="ir.actions.report">
        <field name="name">Recibo de Cobro</field>
        <field name="model">account.payment</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">report_invoice_ride.custom_report_template</field>
        <field name="print_report_name">'Recibo_%s' % (object.name)</field>
        <field name="binding_model_id" ref="account.model_account_payment" />
        <field name="binding_type">report</field>
    </record>

</odoo>